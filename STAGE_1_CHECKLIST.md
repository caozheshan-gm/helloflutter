# Flutter 阶段 1 实战清单（顺序版）

项目路径：`/Users/czss/gitwork/helloflutter/hello_flutter`

- [ ] 修复 `main.dart` 语法问题（`ColorScheme.fromSeed`、`MainAxisAlignment.center`）。
- [ ] 重构首页结构，拆分为 `HomeHeader`、`CounterPanel`、`ActionButtons` 等组件。
- [ ] 建立 `lib/core/theme/`，集中管理颜色、间距、文字样式常量。
- [ ] 新建 `lib/features/todo/`，创建 `todo_page.dart` 与 `todo_item.dart`。
- [ ] 在 Todo 功能中实现新增、删除、完成状态切换。
- [ ] 引入并配置 `shared_preferences`，实现 Todo 本地持久化。
- [ ] 增加基础路由组织（至少首页、Todo 页、设置页）。
- [ ] 在设置页实现一个可切换项（如主题或主色切换）。
- [ ] 为核心逻辑补充基础测试（至少包含一个 widget test）。
- [ ] 每次提交前执行一次 AI review（可读性、命名、重复代码、结构清晰度）。
- [ ] 将阶段总结整理为一份文档，沉淀常见报错与修复方式。

